---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  category: string;
  author: string;
  tags: string[];
}

const { title, description, pubDate, category, author, tags } = Astro.props;
const formattedDate = pubDate.toLocaleDateString("ko-KR", {
  year: "numeric",
  month: "long",
  day: "numeric"
});
---

<BaseLayout title={`${title} - 박원창`} description={description}>
  <article class="article">
    <p class="eyebrow">Engineering Note</p>
    <h1>{title}</h1>
    <p class="lead">{description}</p>
    <p class="article-meta">{category} · {author} · {formattedDate}</p>
    <div class="tag-list" aria-label="Tags">
      {tags.map((tag) => <span class="tag">{tag}</span>)}
    </div>
    <div class="article-body">
      <slot />
    </div>
  </article>
</BaseLayout>
